import { NormalDateParserFormatter } from './normal-date-parser-formatter';

describe('NormalDateParserFormatter', () => {
  it('should create an instance', () => {
    expect(new NormalDateParserFormatter()).toBeTruthy();
  });
});
