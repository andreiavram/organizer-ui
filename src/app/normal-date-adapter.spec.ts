import { NormalDateAdapter } from './normal-date-adapter';

describe('NormalDateAdapter', () => {
  it('should create an instance', () => {
    expect(new NormalDateAdapter()).toBeTruthy();
  });
});
