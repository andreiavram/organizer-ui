import { ReverseLuminanceColorPipe } from './reverse-luminance-color.pipe';

describe('ReverseLuminanceColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseLuminanceColorPipe();
    expect(pipe).toBeTruthy();
  });
});
